// Development-only visual aids. Ship with these OFF.
//
// SHOW_ALIGN_GUIDES draws per-element alignment lines (see
// components/AlignGuides.tsx) for any element marked with `data-guide`.
// It can also be toggled at runtime with Ctrl/Cmd + G. This is a build-time
// switch — there is intentionally no on-screen UI control for it.
export const SHOW_ALIGN_GUIDES = false;
